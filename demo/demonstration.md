# Demonstration

This project supports markdown + generated plots demonstration output.

1. Baseline scenario summary (pilot mode): `outputs/run_001/baseline_summary.md`
2. One-way sensitivity (driver ranking): `outputs/mc_001/tornado.png`
3. Two-way sensitivity (minutes x attrition heatmap): `outputs/mc_001/heatmap_npv.png`
4. Monte Carlo distributions: `outputs/mc_001/mc_summary.csv`
5. Pilot vs scale-up economics: run `twin-econ run` on `configs/base.yaml` and `configs/scaleup_national.yaml` and compare `summary.json` outputs.
